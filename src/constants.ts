import { loadersTypeId } from 'digital-voyager-image-sound-core/lib/constants';

export const MIN_VISUALIZER_WIDTH = 753;
export const MIN_STAGE_WIDTH = 753;
export const MIN_STAGE_HEIGHT = 520;
export const UNKNOWN_BITMAP_TYPE_ID = loadersTypeId.BITMAP_8BITS_PP;
